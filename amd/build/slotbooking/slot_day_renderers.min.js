define("mod_booking/slotbooking/slot_day_renderers",["exports","core/templates"],(function(_exports,_templates){var obj;
/**
   * Shared per-day slot renderers (proportional timeline + flat list) and time helpers.
   *
   * Extracted from condition/slotBooking.js so both the booking and the move flows can render slots
   * the same way (calendar unification — see docs/blueprints/SLOTBOOKING_CALENDAR_UNIFICATION_
   * BLUEPRINT.md).
   *
   * Stage 2: the renderers no longer own the selection. They take a small **selection interface**
   * — { max, isSelected(key), isLocked(key), isCurrent(key), toggle(key), deselect(key) } — so the
   * same renderer works with the booking flow's hidden-input selection (createHiddenInputSelection)
   * and, later, the picker's this.selected/currentKeys/lockedKeys model. The booking flow keeps its
   * exact behaviour via the hidden-input adapter (isLocked/isCurrent always false).
   *
   * @module     mod_booking/slotbooking/slot_day_renderers
   * @copyright  2026 Wunderbyte GmbH
   * @license    http://www.gnu.org/copyleft/gpl.html GNU GPL v3 or later
   */Object.defineProperty(_exports,"__esModule",{value:!0}),_exports.toTimeValue=_exports.renderSlotList=_exports.renderFixedSlotsEditor=_exports.createTimeFormatter=_exports.createHiddenInputSelection=void 0,_templates=(obj=_templates)&&obj.__esModule?obj:{default:obj};_exports.createTimeFormatter=timezone=>{try{return new Intl.DateTimeFormat([],{hour:"2-digit",minute:"2-digit",hour12:!1,timeZone:timezone||void 0})}catch{return new Intl.DateTimeFormat([],{hour:"2-digit",minute:"2-digit",hour12:!1})}};const toTimeValue=(timestamp,formatter)=>{var _parts$find,_parts$find2;const parts=formatter.formatToParts(new Date(1e3*Number(timestamp)));return`${(null===(_parts$find=parts.find((part=>"hour"===part.type)))||void 0===_parts$find?void 0:_parts$find.value)||"00"}:${(null===(_parts$find2=parts.find((part=>"minute"===part.type)))||void 0===_parts$find2?void 0:_parts$find2.value)||"00"}`};_exports.toTimeValue=toTimeValue;_exports.createHiddenInputSelection=(selectionInput,max)=>{const selected=new Set(String(selectionInput.value||"").split(",").map((v=>v.trim())).filter(Boolean)),maxSlots=Math.max(1,Number(max||1));return{max:maxSlots,isSelected:key=>selected.has(key),isLocked:()=>!1,isCurrent:()=>!1,deselect:key=>{selected.delete(key)},toggle(key){if(selected.has(key))selected.delete(key);else{if(maxSlots<=1)selected.clear();else if(selected.size>=maxSlots)return;selected.add(key)}selectionInput.value=Array.from(selected).join(","),selectionInput.dispatchEvent(new Event("change",{bubbles:!0}))}}};const refreshTimelineBlock=(block,selection)=>{if(block.classList.contains("booking-slot--booked")||block.classList.contains("booking-slot--buffer"))return;const key=block.dataset.slotKey;block.classList.remove("booking-slot--available","booking-slot--selected"),block.classList.add(selection.isSelected(key)?"booking-slot--selected":"booking-slot--available"),block.classList.toggle("booking-slot--current",selection.isCurrent(key)),block.classList.toggle("booking-slot--locked",selection.isLocked(key))};_exports.renderFixedSlotsEditor=async(container,daySlots,selection,timeFormatter)=>{var _daySlots$,_daySlots$2;if(container.innerHTML="",!Array.isArray(daySlots)||0===daySlots.length||!selection)return;const hasWarmup=Number((null===(_daySlots$=daySlots[0])||void 0===_daySlots$?void 0:_daySlots$.bufferwarmupminutes)||0)>0,hasCooldown=Number((null===(_daySlots$2=daySlots[0])||void 0===_daySlots$2?void 0:_daySlots$2.buffercooldownminutes)||0)>0,hasBuffer=hasWarmup||hasCooldown,sortedSlots=daySlots.filter((slot=>Number(slot.end||0)>Number(slot.start||0))).sort(((a,b)=>Number(a.start||0)-Number(b.start||0)));if(0===sortedSlots.length)return;const blocks=[];hasWarmup&&blocks.push({key:"buffer:leading",statusclass:"booking-slot--buffer",buffer:!0}),sortedSlots.forEach(((slot,index)=>{const slotStart=Number(slot.start||0),slotEnd=Number(slot.end||0),isBooked="booked"===String(slot.status||"")||!1===slot.selectable,key=String(slot.key||`${slotStart}:${slotEnd}`);isBooked&&selection.deselect(key);const slotPrice=Number(slot.price||0),teacherList=Array.isArray(slot.teachers)?slot.teachers:[];let teachers="";teacherList.length>0&&(teachers=teacherList.length<=2?teacherList.map((t=>String(t.fullname||""))).filter(Boolean).join(", "):"👤 ×"+teacherList.length),blocks.push({key:key,statusclass:isBooked?"booking-slot--booked":selection.isSelected(key)?"booking-slot--selected":"booking-slot--available",time:`${toTimeValue(slotStart,timeFormatter)} - ${toTimeValue(slotEnd,timeFormatter)}`,booked:isBooked,bookedlabel:"Booked",priceformatted:slotPrice>0&&slot.priceformatted?String(slot.priceformatted):"",teachers:teachers});const isLast=index===sortedSlots.length-1;(!isLast&&hasBuffer||isLast&&hasCooldown)&&blocks.push({key:`buffer:${key}:trailing`,statusclass:"booking-slot--buffer",buffer:!0})}));const{html:html,js:js}=await _templates.default.renderForPromise("mod_booking/slotbooking/slot_grid_day",{blocks:blocks});_templates.default.replaceNodeContents(container,html,js),container.querySelectorAll(".booking-slot").forEach((block=>{if(refreshTimelineBlock(block,selection),block.classList.contains("booking-slot--booked")||block.classList.contains("booking-slot--buffer"))return;const key=block.dataset.slotKey;block.addEventListener("click",(()=>{selection.isLocked(key)||(selection.toggle(key),container.querySelectorAll(".booking-slot").forEach((b=>refreshTimelineBlock(b,selection))))}))}))};_exports.renderSlotList=async(container,slots,selection)=>{if(!Array.isArray(slots)||0===slots.length||!selection)return void(container.innerHTML="");const items=[];slots.forEach((slot=>{const slotStart=Number(slot.start||0),slotEnd=Number(slot.end||0);if(slotEnd<=slotStart)return;const key=String(slot.key||`${slotStart}:${slotEnd}`),isBooked="booked"===String(slot.status||"")||!1===slot.selectable;isBooked&&selection.deselect(key);const slotPrice=Number(slot.price||0);items.push({key:key,label:`${slot.daylabel||""} - ${slot.timelabel||key}`,priceformatted:slotPrice>0&&slot.priceformatted?String(slot.priceformatted):"",selected:!isBooked&&selection.isSelected(key),booked:isBooked})}));const{html:html,js:js}=await _templates.default.renderForPromise("mod_booking/slotbooking/slot_day_list",{items:items});_templates.default.replaceNodeContents(container,html,js),container.querySelectorAll(".booking-slot-list-item").forEach((item=>{const key=item.dataset.slotKey;item.classList.toggle("booking-slot-list-item--current",!item.classList.contains("booking-slot-list-item--booked")&&selection.isCurrent(key)),item.classList.toggle("booking-slot-list-item--locked",selection.isLocked(key)),item.classList.contains("booking-slot-list-item--booked")||item.addEventListener("click",(()=>{selection.isLocked(key)||(selection.toggle(key),container.querySelectorAll(".booking-slot-list-item").forEach((el=>{el.classList.contains("booking-slot-list-item--booked")||el.classList.toggle("booking-slot-list-item--selected",selection.isSelected(el.dataset.slotKey))})))}))}))}}));

//# sourceMappingURL=slot_day_renderers.min.js.map