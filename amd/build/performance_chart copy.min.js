/**
 * AJAX helper for the inline editing a value.
 *
 * This script is automatically included from template core/inplace_editable
 * It registers a click-listener on [data-inplaceeditablelink] link (the "inplace edit" icon),
 * then replaces the displayed value with an input field. On "Enter" it sends a request
 * to web service core_update_inplace_editable, which invokes the specified callback.
 * Any exception thrown by the web service (or callback) is displayed as an error popup.
 *
 * @module     mod_booking/performance_chart
 * @copyright  2018 David Bogner
 * @license    http://www.gnu.org/copyleft/gpl.html GNU GPL v3 or later
 * @since      3.3
 */
define("mod_booking/performance_chart copy",["core/chartjs"],function(Chart){return{init:(canvasId,dataScriptId)=>{const canvas=document.getElementById(canvasId);if(!canvas)return;const dataNode=document.getElementById(dataScriptId);if(!dataNode)return;let parsed;try{parsed=JSON.parse(dataNode.textContent)}catch(e){return}const labels=parsed.labels||[],datasets=(parsed.datasets||[]).map(ds=>({label:ds.label,data:ds.data,backgroundColor:ds.backgroundColor}));new Chart(canvas.getContext("2d"),{type:"bar",data:{labels:labels,datasets:datasets},options:{responsive:!0,maintainAspectRatio:!1,plugins:{legend:{position:"right",align:"start",labels:{boxWidth:16,padding:12}},tooltip:{enabled:!0}},scales:{x:{title:{display:!1}},y:{beginAtZero:!0,title:{display:!0,text:"Time (ms)"}}}}})}}});

//# sourceMappingURL=performance_chart copy.min.js.map